![alt text](<Screenshot 2026-07-24 183949.png>)

Правильными ответами являются три основных типа архитектуры хранения данных:

DAS (Direct Attached Storage)

NAS (Network Attached Storage)

SAN (Storage Area Network)

Пояснение:

iSCSI и Fiber Channel (FC) — это сетевые протоколы передачи данных, используемые для подключения к системам хранения (преимущественно к SAN), а не самостоятельные типы решений для хранения данных.

![alt text](<Screenshot 2026-07-24 184106.png>)

Правильным ответом является: 128

Пояснение:

В операционных системах Windows разметка GPT (GUID Partition Table) по умолчанию поддерживает создание до 128 основных разделов. Для сравнения, старая разметка MBR позволяет создать только 4 основных раздела.

![alt text](<Screenshot 2026-07-24 184136.png>)

Правильным ответом является:

Включить опцию Enable spoofing of MAC addresses на уровне адаптеров виртуальных машин

Пояснение:

При настройке NLB-кластера в режиме unicast (одноадресный режим) все узлы кластера начинают использовать один и тот же виртуальный MAC-адрес. 

Виртуальные машины (например, в Hyper-V) по соображениям безопасности по умолчанию блокируют отправку сетевых пакетов с MAC-адресом, отличным от их первоначального статического или динамического адреса.Чтобы кластер успешно функционировал в виртуальной среде Hyper-V, в настройках сетевого адаптера каждой виртуальной машины необходимо явно активировать разрешение на подмену MAC-адреса (Enable spoofing of MAC addresses).

![alt text](<Screenshot 2026-07-24 184237.png>)

Правильными ответами являются:

VDI (Virtual Desktop Infrastructure)Server virtualization (Виртуализация серверов)

Пояснение:

Роль Hyper-V является аппаратным гипервизором Microsoft и напрямую используется для создания и управления виртуальными машинами — как в инфраструктуре виртуальных рабочих столов (VDI), так и для классической виртуализации серверов (Server virtualization). Технологии виртуализации приложений (Application virtualization, например, App-V) или локальной виртуализации десктопов (Desktop virtualization в контексте клиентских ОС) не требуют обязательного развертывания серверной роли Hyper-V.

![alt text](<Screenshot 2026-07-24 184310.png>)

Правильным ответом является: 2

Пояснение:

Составной том (Spanned volume) в Windows Server объединяет области свободного пространства на нескольких физических дисках в один логический том. Чтобы начать такое объединение, требуется как минимум два динамических диска.

![alt text](<Screenshot 2026-07-24 184327.png>)

Правильным ответом является: sconfig

Пояснение:

Утилита sconfig (Server Configuration) представляет собой встроенный текстовый инструмент меню для выполнения первоначальной настройки серверов под управлением Windows Server в режиме Core. С её помощью можно быстро настроить сетевые параметры, изменить имя компьютера, присоединить его к домену или настроить обновления.

![alt text](<Screenshot 2026-07-24 184413.png>)

Правильным ответом является:

Протокол UDP не ориентирован на установление соединения и работает на транспортном уровне стека TCP/IP

Пояснение:

Протокол UDP (User Datagram Protocol) является протоколом без установления соединения (connectionless), что отличает его от TCP. При этом оба они выполняют свои функции на транспортном уровне модели TCP/IP.

![alt text](<Screenshot 2026-07-24 184433.png>)

Правильным ответом является: 4

Пояснение:

Классический стек протоколов TCP/IP (согласно стандартам RFC) состоит из 4 уровней: 

прикладного (Application), 

транспортного (Transport), 

межсетевого (Internet) и уровня сетевого доступа (Network Access). 

7 уровней имеет модель OSI, а не TCP/IP.

![alt text](<Screenshot 2026-07-24 184513.png>)

Правильным ответом является: одноранговой 

Пояснение:

В одноранговой сети (Peer-to-Peer, P2P) все компьютеры равны в правах, и каждый из них может одновременно предоставлять свои ресурсы другим участникам (работать как сервер) и потреблять чужие ресурсы (работать как клиент).

![alt text](<Screenshot 2026-07-24 184553.png>)

Правильным ответом является: ipconfig

Пояснение:

На скриншоте показан классический вывод консольной утилиты ipconfig в операционной системе Windows. Именно она используется для просмотра текущих настроек протокола IP сетевых адаптеров компьютера (IP-адрес, маска подсети, основной шлюз).

![alt text](<Screenshot 2026-07-24 184620.png>)

Правильным ответом является: DHCP

Пояснение:

Протокол DHCP (Dynamic Host Configuration Protocol) разработан специально для автоматического назначения сетевых параметров (IP-адреса, маски подсети, основного шлюза и DNS-серверов) узлам в сети.

![alt text](<Screenshot 2026-07-24 184639.png>)

Правильными ответами являются: 

файл Hosts

DNS-сервер

Пояснение:Для преобразования доменных (DNS) имен в IP-адреса используются локальный текстовый файл Hosts, который имеет наивысший приоритет на компьютере, и централизованный DNS-сервер. Файл Lmhosts служит для разрешения имен NetBIOS, а DHCP-сервер отвечает за выдачу сетевых настроек.

![alt text](<Screenshot 2026-07-24 184703.png>)

Правильным ответом является:

DSA

Пояснение:

DSA (Digital Signature Algorithm) — это алгоритм асимметричного шифрования с открытым ключом, который применяется для создания и проверки электронных цифровых подписей. Все остальные варианты в списке (SEAL, DES, 3DES, AES) относятся к алгоритмам симметричного шифрования.

![alt text](<Screenshot 2026-07-24 184949.png>)

Правильным ответом является:

Канал EtherChannel будет по-прежнему функционировать.

Пояснение:

Технология EtherChannel агрегирует несколько физических каналов в один логический для обеспечения избыточности и отказоустойчивости. Если один из четырех физических интерфейсов выйдет из строя, трафик автоматически перераспределится между оставшимися тремя рабочими линиями, а сам логический канал EtherChannel продолжит работу без разрыва соединения.

![alt text](<Screenshot 2026-07-24 185014.png>)

Правильным ответом является: 62

Пояснение:

Маска подсети /26 оставляет под адресацию узлов \(32 - 26 = 6\) бит. 

Общее количество адресов в такой подсети составляет \(2^6 = 64\). 

Чтобы узнать число действительных (доступных для назначения устройствам) адресов, необходимо вычесть 2 адреса: один под адрес самой подсети, а второй под широковещательный адрес (Broadcast). 

Получаем: \(64 - 2 = 62\).

![alt text](<Screenshot 2026-07-24 185045.png>)

Правильными ответами являются: 172.16.15.36 172.16.0.255 

Пояснение:

Команда использует обратную маску (wildcard mask) 0.0.15.255. Это означает, что первые два октета должны строго соответствовать 172.16, в третьем октете проверяются только биты, где в маске стоят нули (диапазон значений от 0 до 15), а четвертый октет может быть любым (0-255). Таким образом, под это правило подходят только адреса, у которых третий октет находится в промежутке от 0 до 15 включительно.

![alt text](<Screenshot 2026-07-24 185109.png>)

Правильным ответом является:

Это нормальное поведение для сети 2-го уровня.

Пояснение:

Поле TTL (Time to Live) находится в заголовке IP-пакета, который относится к 3-му (сетевому) уровню модели OSI. 

Коммутаторы 2-го уровня (Data Link layer) работают только с кадрами (фреймами) и MAC-адресами, они пересылают пакеты внутри одной локальной сети и не изменяют содержимое IP-заголовка. Значение TTL уменьшают только маршрутизаторы (устройства 3-го уровня) при пересылке пакета между разными подсетями.

![alt text](<Screenshot 2026-07-24 185228.png>)

Правильным ответом является: Отбросит кадр.

Пояснение:

Сетевой адаптер конечного узла в сети Ethernet проверяет MAC-адрес назначения в заголовке входящего кадра. Если этот адрес не совпадает с его собственным физическим адресом (и не является широковещательным/многоадресным), устройство понимает, что данные предназначены не ему, и просто отбрасывает (игнорирует) этот кадр на канальном уровне.

![alt text](<Screenshot 2026-07-24 185251.png>)

Правильным ответом является: fe80::1 

Пояснение:

Локальные адреса канала IPv6 (Link-Local Addresses) всегда используют строго определенный диапазон и начинаются с префикса fe80::/10. Для сравнения: адреса, начинающиеся с ff02::, являются многоадресными (Multicast), а 2001: относится к глобальным уникальным адресам (Global Unicast).

![alt text](<Screenshot 2026-07-24 185324.png>)

Правильным ответом является: IPsec

Пояснение:

Протокол IPsec (особенно в режиме AH или при использовании ESP без NAT-Traversal) сталкивается с проблемами при прохождении NAT, так как NAT изменяет IP-адреса и порты в заголовках пакетов. Это нарушает целостность данных, контролируемую криптографическими хэшами IPsec, из-за чего пакеты отбрасываются принимающей стороной. Остальные протоколы (Telnet, HTTP, ICMP, DNS) работают через NAT без подобных фундаментальных конфликтов.